import { FilterDefinition } from "./filterdefinition";

describe("FilterDefinition", () => {
  it("should create an instance", () => {
    expect(new FilterDefinition()).toBeTruthy();
  });
});
