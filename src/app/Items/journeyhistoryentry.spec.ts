import { JourneyHistoryEntry } from "./journeyhistoryentry";

describe("JourneyHistoryEntry", () => {
  it("should create an instance", () => {
    expect(new JourneyHistoryEntry()).toBeTruthy();
  });
});
